services:
  - type: web
    name: bfhl-api
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/bfhl-api-1.0.0.jar
    envVars:
      - key: PORT
        value: 8080
